# 📋 О проекте
Этот проект предназначен для конвертации DVD-видео из структуры VIDEO_TS в современный формат MP4 с сохранением высокого качества. Скрипты автоматически объединяют разбитые VOB-файлы в один цельный MP4-файл.

Особенности:

✅ Автоматическое объединение VOB-файлов

✅ Сохранение высокого качества видео

✅ Поддержка русских названий файлов и путей

✅ Автоматический поиск FFmpeg в системе

✅ Простота использования

# 📦 Требования
Windows (7/8/10/11)

Python 3.7+ (только для установки FFmpeg, сам скрипт не требует Python)

FFmpeg (устанавливается автоматически через pip)

# 🚀 Быстрый старт
1. Установка FFmpeg
Откройте командную строку (cmd) или PowerShell и выполните:

pip install imageio-ffmpeg

Или если pip не установлен:

python -m pip install imageio-ffmpeg

2. Скачивание скрипта

3. Настройка PowerShell (только один раз)

Запустите PowerShell от имени администратора и выполните:

Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
4. Использование

.\convert.ps1 "C:\путь\к\папке\с\видео"
Пример:

.\convert.ps1 "C:\АРХИВ ПЕРЕЗАГРУЗКИ\Перезагрузка ХР 2009"

# 🎛️ Настройка качества
В скрипте можно изменить параметр -crf (Constant Rate Factor):

CRF	Качество	Размер файла	Рекомендация

18	Отличное	Большой	Для архивации

23	Хорошее	Средний	По умолчанию (рекомендуется)

28	Среднее	Маленький	Для мобильных устройств

Чтобы изменить качество, отредактируйте строку в скрипте:

-crf 23  # Замените на нужное значение (18, 23 или 28)

# 📊 Процесс конвертации
Скрипт находит все VOB файлы в папке VIDEO_TS

Автоматически исключает файлы меню (содержащие 0.VOB)

Объединяет все части фильма в один поток

Конвертирует в MP4 с кодеком H.264 и AAC

Сохраняет результат в родительскую папку

Время конвертации: Примерно 30-60 минут для стандартного DVD (в зависимости от мощности компьютера).

# 📝 Лицензия
Скрипт предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск.

# 🤝 Поддержка
Если у вас возникли проблемы:

Проверьте правильность установки FFmpeg

Убедитесь, что папка содержит VIDEO_TS с VOB файлами

Запустите PowerShell от имени администратора

Используйте прямые команды вместо скрипта

Приятного просмотра! 🎬
